# AGENTS.md

- Do not assume the sandbox environment reflects the user's actual local environment, installed libraries, tooling, or workflow.
- When refactoring or translating an existing script or notebook, preserve the original libraries and logic unless the user explicitly asks for a change.
- If sandbox limitations prevent verification, state that clearly instead of replacing the user's dependencies or redesigning the implementation.
